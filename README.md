# QR Code Generator

This is a Python project that generates a QR code for a given URL or text input.

The program takes user input and creates a QR code image that can be scanned to access the link or information.

## Technologies Used

* Python
* qrcode library
* Pillow
